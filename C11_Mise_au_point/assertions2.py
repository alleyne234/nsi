from random import randint

def liste_uniques(N, debut, fin):
    """Renvoie une liste de N nombres entiers aléatoires entre debut et fin sans aucun doublon.
    """
    tab = []
    while len(tab) < N:
        valeur = randint(debut, fin)
        if valeurnot in tab:
            
